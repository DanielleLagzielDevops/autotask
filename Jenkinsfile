pipeline {
    //def app
    agent { dockerfile true } 
    //agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'Test'
                //checkout scm
            }
        }
    }
}
        //new
        //stage('Build dockerfile') {
            
        //    docker.build("daniellelagziel/autotask")
        //}
        // stage('Test') {
         //    app.inside {
          //      sh 'echo "Test pass"'
           //  }
        //}
   // }
//}
